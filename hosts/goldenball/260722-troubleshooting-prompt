You are working in `/home/r6t/git/nixos-r6t`, a NixOS flake repo. Continue troubleshooting `goldenball`, an ASUS ROG Flow Z13 GZ302EA with AMD Ryzen AI MAX+ 395 / Strix Halo / Radeon 8060S. The user is considering abandoning NixOS on this hardware unless stability improves.

Hard constraints from repo instructions:
- Read `docs/GOLDENBALL_FREEZES.md` first.
- Use the `host-troubleshooting` skill/workflow if available.
- Only modify files inside this flake git project.
- Never run `nixos-rebuild`, `nix build`, `home-manager switch`, or any Nix command that builds/activates configurations.
- Never run `containers/build.py` or `containers/relaunch.py`.
- Never run `nix flake update`; never modify `flake.lock`.
- Never run `git add`, `git commit`, or `git push`.
- After flake edits, run `./format.fish`.
- Preserve unrelated worktree changes.

Current important context:
- `goldenball` hard-freezes are documented in `docs/GOLDENBALL_FREEZES.md`.
- Primary signature is amdgpu/DCN display engine wedging:
  - `flip_done timed out`
  - KWin: `Pageflip timed out! This is a bug in the amdgpu kernel driver`
  - later `commit wait timed out`, `vblank wait timed out`
- Device:
  - ASUS ROG Flow Z13 GZ302EA
  - AMD Ryzen AI MAX+ 395, Strix Halo, gfx1151/RDNA 3.5/DCN 3.5.1
  - 128 GB unified RAM
  - internal OLED eDP-1 2560x1600 48-180 Hz
  - external display sometimes via Plugable USB4-HUB3A/TB4 dock at 4K240
- User specifically points out: other hosts at the same desk never have issues with this dock. Treat “the dock is bad” as unlikely. USB4/DP tunnel instability may still be a goldenball-specific trigger, but the main target should be kernel amdgpu/DCN behavior.

Current config already has many mitigations in `hosts/goldenball/configuration.nix`:
- `amdgpu.dcdebugmask=0x1613`
- `amdgpu.sg_display=0`
- `amdgpu.gpu_recovery=1`
- `amdgpu.ppfeaturemask=0xfff73fff`
- `amdgpu.freesync_video=0`
- `amdgpu.aspm=0`
- `amdgpu.abmlevel=0`
- `pcie_aspm=off`
- `pcie_port_pm=off`
- `pcie_ports=native`
- `pci=realloc`
- `thunderbolt.clx=0`
- `iommu=pt`
- `ttm.pages_limit=27262976`
- `amd_pstate=guided`
- modprobe: `options mt7925e disable_aspm=1`
- modprobe: `options amdgpu cwsr_enable=0`
- KWin VRR disabled: `VrrPolicy=0`
- KWin overlays disabled: `KWIN_DRM_NO_OVERLAY=1`

Recent changes already made in this working tree:
- `hosts/goldenball/configuration.nix`
  - added `boot.initrd.kernelModules = [ "amdgpu" ];` for early KMS so amdgpu initializes before Thunderbolt/USB4 DP tunnel creation
  - added kernel param `amdgpu.cwsr_enable=0` because early initrd amdgpu load needs cmdline/module parameter availability before stage 2 modprobe config
  - added `networking.networkmanager.wifi.powersave = false;`
- `modules/nixos/steam/config.nix`
  - goldenball gamescope launcher profiles now pass `--nested-refresh 180`, not 120. This is intended to preserve the tablet’s 180 Hz refresh while avoiding gamescope picking the external 4K240 path.
- `docs/GOLDENBALL_FREEZES.md`
  - updated to document the Jul 22 2026 baseline.
- `./format.fish` passed after these changes.
- Read-only `nix eval` checks confirmed:
  - `networking.networkmanager.wifi.powersave = false`
  - initrd modules include `amdgpu`
  - kernel params include `amdgpu.cwsr_enable=0`
- No build/rebuild/activation was run.

Research already checked:
- https://forum.level1techs.com/t/asus-z13-flow-strix-halo-arch-linux-set-up-fall-2025/236651
  - Mostly community Z13/Arch discussion; mentions Bazzite/CachyOS, Antheas ASUS enablement patches, WiFi/audio/BT annoyances.
- https://www.phoronix.com/news/ASUS-Z13-ROG-Ally-RGB
  - ASUS HID/platform patches: fan key, multitouch, RGB. Not directly about amdgpu/DCN freezes.
- https://github.com/ib99/ASUS-ROG-Flow-Z13-2025-Linux-Guide-Omarchy-CachyOS-Kernel
  - Recommends CachyOS kernel, asusctl, linux-firmware-git, `options mt7925e disable_aspm=1`, AMD pstate EPP.
- https://th3cavalry.github.io/strix-halo-linux-setup/
  - Most relevant community Strix Halo setup.
  - Recommends GPU/display mitigations overlapping current config: `dcdebugmask`, `sg_display=0`, `abmlevel=0`, `cwsr_enable=0`.
  - Its current docs recommend narrower `amdgpu.dcdebugmask=0x600`, but goldenball local evidence led to broader `0x1613`; do not relax this without strong reason.
  - Its `gpu-manager.sh` enables early KMS.
- https://forum.level1techs.com/t/the-ultimate-arch-secureboot-guide-for-ryzen-ai-max-ft-hp-g1a-128gb-8060s-monster-laptop/230652
  - Useful Strix Halo context; less specific to Z13 DCN freezes.
  - Mentions MT7925/IPv6/ASPM issues and general kernel-version churn.

Your task:
1. Inspect the current repo state and the three modified files above.
2. Re-read `docs/GOLDENBALL_FREEZES.md` carefully and distinguish confirmed observations from hypotheses.
3. Continue research with emphasis on upstream amdgpu/DCN/DMUB/DCN 3.5.1/Strix Halo display issues, not generic dock blame.
4. Search for current upstream kernel activity/issues:
   - `drm/amd flip_done timed out DCN 3.5.1`
   - `Strix Halo amdgpu flip_done timed out`
   - `Radeon 8060S DCN 3.5.1`
   - `gfx1151 amdgpu display freeze`
   - `amdgpu DMUB HPD IRQ callback link_index flip_done`
   - `amdgpu DC_DISABLE_MPO pipe split PSR replay Strix Halo`
   - freedesktop GitLab `drm/amd` issues around Strix Halo, DCN 3.5, pageflip timeout
   - Linux kernel commits in `drivers/gpu/drm/amd/display/` since 2026-06, especially DCN 3.5.x, DMUB, PSR, Panel Replay, DPIA, USB4/DP tunneling, MST/HPD.
5. Compare any findings to current NixOS config. Prefer minimal, justified flake changes only.
6. If you make changes, update `docs/GOLDENBALL_FREEZES.md` with the exact reasoning and run `./format.fish`.
7. Do not run builds or activation commands.

What to be careful about:
- Do not suggest that the Plugable dock is generally bad; user reports other hosts are stable with it.
- USB4/TB events may be preconditions/triggers on this host, but fatal signature is still amdgpu/DCN display pipeline wedge.
- Preserve `amdgpu.dcdebugmask=0x1613` unless you find a strong reason to change it. th3cavalry’s `0x600` is narrower, but goldenball has specific history showing broader mitigations reduced freezes.
- Do not cap tablet gaming below 180 Hz. The user wants 180 Hz on the tablet screen.
- Avoid huge distro-style changes unless presenting them as an explicit tradeoff. User is deciding whether NixOS remains viable, but wants flake refinement if possible.
- If considering CachyOS/Fedora/Bazzite, separate “better defaults/kernel patch cadence” from “NixOS caused the bug.” The evidence currently points to kernel amdgpu/DCN behavior.

Useful files:
- `docs/GOLDENBALL_FREEZES.md`
- `hosts/goldenball/configuration.nix`
- `hosts/goldenball/hardware-configuration.nix`
- `modules/nixos/steam/config.nix`
- `modules/nixos/usb4-sfp/config.nix`
- `modules/nixos/networkmanager/config.nix`
- `hosts/goldenball/llm-config.nix`
- `modules/nixos/llama-cpp/config.nix`

Potential useful deliverables:
- A short prioritized list of next flake changes, each tied to evidence.
- An upstream `drm/amd` bug report draft with exact logs/signatures from `docs/GOLDENBALL_FREEZES.md`.
- A “NixOS vs Bazzite/CachyOS/Fedora” assessment focused on whether they likely carry newer kernels, Mesa, firmware, or out-of-tree amdgpu/display patches that NixOS lacks.
- If no further flake changes are justified, say that directly and focus on upstream reporting / kernel A-B testing strategy.
