---
name: theming
description: Use for shared colors, palettes, oxocarbon styling, terminal or application themes, and keeping theme values consistent across this repository.
---

# Theming

Read `docs/THEMING.md` before making changes.

Use the existing shared palette and application-specific adapters. Avoid duplicating color literals when an established palette value exists.

After edits, run `./format.fish`; never run a Nix build or activation command.
